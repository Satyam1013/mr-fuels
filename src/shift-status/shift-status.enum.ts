export enum ClosedBy {
  MANAGER = "manager",
  ADMIN = "admin",
}

export enum ShiftStatusEnum {
  ACTIVE = "active",
  PENDING = "pending",
  COMPLETED = "completed",
}

export enum PumpStatusEnum {
  OPEN = "open",
  CLOSED = "closed",
  HOLIDAY = "holiday",
}
