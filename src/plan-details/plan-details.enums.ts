export enum PlanName {
  PRO = "pro",
  PREMIUM = "premium",
}

export enum PlanStatus {
  ACTIVE = "active",
  INACTIVE = "inactive",
}

export enum DurationType {
  TRIAL = "trial",
  MONTHLY = "monthly",
  YEARLY = "yearly",
}

export enum Currency {
  INR = "INR",
}
