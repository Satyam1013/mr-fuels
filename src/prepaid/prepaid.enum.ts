export enum ShiftStatus {
  OPEN = "open",
  CLOSED = "closed",
}

export enum PrepaidModeEnum {
  DEPOSIT = "deposit",
  TRANSIT = "transit",
}

export enum PrepaidProductTypeEnum {
  FUEL = "fuelType",
  NON_FUEL = "nonFuelType",
}
