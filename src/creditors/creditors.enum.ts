export enum CreditBy {
  STAFF = "staff",
  MANAGER = "manager",
  ADMIN = "admin",
}

export enum CreditStatusEnum {
  TAKEN = "taken",
  RETURNED = "returned",
}

export enum ReturnPaymentModeEnum {
  UPI = "upi",
  POS = "pos",
  CASH = "cash",
}
